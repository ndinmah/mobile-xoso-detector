spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate
logging.level.root=INFO
logging.level.com.example.xoso=DEBUG

# Render injects PORT env var, tell Spring Boot to use it
server.port=${PORT:8080}
